# 无碍e家 网站实现规划

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 搭建纯静态无障碍家居信息网站，包含改造家居（搜索+分页）、政策 PDF、经验分享（分页），蓝白简约风格。

**Architecture:** Astro 静态站点，Tailwind 蓝白主题，JSON 数据源，Pagefind 搜索，客户端分页与筛选。数据由 xls/docx 一次性转为 JSON。

**Tech Stack:** Astro 5.x, Tailwind CSS 4.x, Pagefind（搜索）, @astrojs/vercel（部署）

**参考:** `docs/brainstorms/2025-03-01-无障碍家居网站-brainstorm.md`

---

## 数据 Schema

### 改造家居产品 `src/data/products.json`
```json
[
  {
    "id": "1",
    "name": "厨房低位操作台",
    "category": "肢体类",
    "venue": "厨房",
    "audience": "轮椅使用者",
    "material": "材质与功能描述...",
    "image": "/images/products/placeholder.svg"
  }
]
```

### 书籍 `src/data/books.json`
```json
[
  {
    "id": "1",
    "title": "无障碍的家",
    "audience": "肢体类障碍人群",
    "summary": "本书系统阐述...",
    "author": "作者名",
    "cover": "/images/books/placeholder.svg"
  }
]
```

### 政策 `src/data/policies.json`
```json
[
  {
    "id": "1",
    "title": "中华人民共和国无障碍环境建设法",
    "file": "/policies/中华人民共和国无障碍环境建设法.pdf"
  }
]
```

---

## Phase 0: 环境准备

### Task 0.1: 初始化 Astro 项目

**Files:**
- Create: `package.json`（通过 npm create astro）
- Create: `astro.config.mjs`
- Create: `tsconfig.json`

**Step 1:** 创建 Astro 项目（含 Tailwind）

```bash
cd /Users/cyibin/Documents/first-year/programs/Accessible_books
npm create astro@latest . -- --template minimal --install --no-git
npx astro add tailwind
```

**Step 2:** 验证

```bash
npm run dev
```

Expected: 开发服务器启动，访问 http://localhost:4321 可见示例页

**Step 3:** Commit

```bash
git add . && git commit -m "chore: init Astro + Tailwind"
```

---

### Task 0.2: 配置 Tailwind 蓝白主题

**Files:**
- Modify: `tailwind.config.mjs` 或 `src/styles/global.css`（Tailwind v4 在 CSS 中配置）

**Step 1:** 在 `src/styles/global.css` 中定义主题色（若 Tailwind v4）

```css
@import "tailwindcss";

@theme {
  --color-primary: #2563eb;
  --color-primary-light: #3b82f6;
  --color-bg: #f8fafc;
  --color-surface: #ffffff;
}
```

**Step 2:** 验证首页可应用新颜色（在 BaseLayout 中加 `bg-bg` 等 class）

**Step 3:** Commit

```bash
git add . && git commit -m "style: add blue-white theme tokens"
```

---

## Phase 1: 数据准备

### Task 1.1: 编写 xls → products.json 转换脚本

**Files:**
- Create: `scripts/xls-to-products.js`

**Step 1:** 创建脚本

```javascript
// scripts/xls-to-products.js
import XLSX from 'xlsx';
import { writeFileSync } from 'fs';
import { dirname, join } from 'path';
import { fileURLToPath } from 'url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const xlsPath = join(__dirname, '../data/残疾人家居改造家具分类表(1).xls');
const outPath = join(__dirname, '../src/data/products.json');

const wb = XLSX.readFile(xlsPath);
const ws = wb.Sheets[wb.SheetNames[0]];
const rows = XLSX.utils.sheet_to_json(ws, { header: 1 });

const products = [];
let category = '肢体类';
const headerRow = rows.findIndex(r => r && (r[0] === '序号' || r[1] === '适用场所'));
if (headerRow < 0) throw new Error('Header row not found');

for (let i = headerRow + 1; i < rows.length; i++) {
  const r = rows[i];
  if (!r || r.every(c => c == null || c === '')) continue;
  if (['肢体类', '视障类', '听障类'].includes(r[0])) { category = r[0]; continue; }
  if (r[0] === '序号' || !r[0]) continue;
  products.push({
    id: String(products.length + 1),
    name: r[2] || '',
    category,
    venue: r[1] || '',
    audience: String(r[4] || '').replace(/\n/g, '、'),
    material: String(r[3] || '').replace(/\n/g, ' '),
    image: '/images/products/placeholder.svg'
  });
}

writeFileSync(outPath, JSON.stringify(products, null, 2), 'utf-8');
console.log(`Wrote ${products.length} products to ${outPath}`);
```

**Step 2:** 安装依赖（在项目根目录）

```bash
npm install xlsx
```

若 `package.json` 无 `"type": "module"`，可将脚本改为 `.mjs` 后缀或添加 `"type": "module"`。

**Step 3:** 创建 `src/data` 目录并运行

```bash
mkdir -p src/data
node scripts/xls-to-products.js
```

Expected: 生成 `src/data/products.json`

**Step 4:** Commit

```bash
git add . && git commit -m "feat: add xls to products.json script"
```

---

### Task 1.2: 准备 books.json

**Files:**
- Create: `src/data/books.json`
- Create: `scripts/docx-to-books.py`（可选，用 Python 解析 docx）

**方案 A（推荐）：** 用 Python 解析 docx 输出 JSON

```python
# scripts/docx-to-books.py
# python -c "from docx import Document; ..." 或单独脚本
# 解析 1. 书名 2. 适用人群 3. 简介 段落结构，输出 books.json
```

**方案 B：** 手动整理。根据 `data/无障碍相关书籍.docx` 内容，直接编写 `src/data/books.json`（至少 3–5 本占位）。

**Step 1:** 创建 `src/data/books.json`，格式见上文 Schema

**Step 2:** 若采用 Python，运行 `python scripts/docx-to-books.py` 覆盖生成

**Step 3:** Commit

```bash
git add . && git commit -m "feat: add books data"
```

---

### Task 1.3: 生成 policies.json 并复制 PDF

**Files:**
- Create: `scripts/gen-policies.js`
- Create: `public/policies/`（PDF 存放）

**Step 1:** 创建脚本，扫描 `政策文件/` 生成 `src/data/policies.json`

```javascript
// scripts/gen-policies.js
import { readdirSync, copyFileSync, mkdirSync, existsSync, writeFileSync } from 'fs';
import { join, dirname } from 'path';
import { fileURLToPath } from 'url';

const __dirname = dirname(fileURLToPath(import.meta.url));
const srcDir = join(__dirname, '../政策文件');
const destDir = join(__dirname, '../public/policies');
const outPath = join(__dirname, '../src/data/policies.json');

if (!existsSync(destDir)) mkdirSync(destDir, { recursive: true });
const files = readdirSync(srcDir).filter(f => f.endsWith('.pdf'));
const policies = files.map((f, i) => {
  const src = join(srcDir, f);
  const dest = join(destDir, f);
  if (existsSync(src)) copyFileSync(src, dest);
  return { id: String(i + 1), title: f.replace('.pdf', ''), file: `/policies/${f}` };
});
writeFileSync(outPath, JSON.stringify(policies, null, 2), 'utf-8');
console.log(`Wrote ${policies.length} policies`);
```

**Step 2:** 运行

```bash
node scripts/gen-policies.js
```

**Step 3:** Commit

```bash
git add . && git commit -m "feat: add policies data and copy PDFs"
```

---

## Phase 2: 布局与首页

### Task 2.1: 创建 BaseLayout

**Files:**
- Create: `src/layouts/BaseLayout.astro`
- Modify: `src/styles/global.css`（确保已引入）

**Step 1:** 创建布局

```astro
---
import '../styles/global.css';
interface Props { title?: string; }
const { title = '无碍e家' } = Astro.props;
---
<!DOCTYPE html>
<html lang="zh-CN">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>{title}</title>
  <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
</head>
<body class="min-h-screen bg-slate-50 text-slate-800">
  <header class="bg-white border-b border-slate-200 py-4 px-6">
    <a href="/" class="text-xl font-semibold text-blue-600 hover:text-blue-700">无碍e家</a>
  </header>
  <main class="max-w-5xl mx-auto px-6 py-8">
    <slot />
  </main>
  <footer class="border-t border-slate-200 py-6 px-6 text-center text-slate-500 text-sm">
    无障碍家居改造信息平台
  </footer>
</body>
</html>
```

**Step 2:** 验证（在 index 中使用）

**Step 3:** Commit

```bash
git add . && git commit -m "feat: add BaseLayout"
```

---

### Task 2.2: 实现首页

**Files:**
- Modify: `src/pages/index.astro`

**Step 1:** 实现三个入口卡片

```astro
---
import BaseLayout from '../layouts/BaseLayout.astro';
---
<BaseLayout>
  <h1 class="text-2xl font-bold text-slate-800 mb-8">无碍e家</h1>
  <p class="text-slate-600 mb-10">了解更多无障碍家居改造信息</p>
  <div class="grid gap-6 sm:grid-cols-2 lg:grid-cols-3">
    <a href="/products" class="block p-6 bg-white rounded-lg border border-slate-200 hover:border-blue-300 hover:shadow-md transition">
      <h2 class="text-lg font-semibold text-blue-600">改造家居</h2>
      <p class="mt-2 text-slate-600 text-sm">肢体类、视障类、听障类产品</p>
    </a>
    <a href="/policies" class="block p-6 bg-white rounded-lg border border-slate-200 hover:border-blue-300 hover:shadow-md transition">
      <h2 class="text-lg font-semibold text-blue-600">家居改造政策信息</h2>
      <p class="mt-2 text-slate-600 text-sm">政策文件与法规</p>
    </a>
    <a href="/books" class="block p-6 bg-white rounded-lg border border-slate-200 hover:border-blue-300 hover:shadow-md transition">
      <h2 class="text-lg font-semibold text-blue-600">经验分享</h2>
      <p class="mt-2 text-slate-600 text-sm">无障碍改造相关书籍</p>
    </a>
  </div>
</BaseLayout>
```

**Step 2:** 验证 `npm run dev` 首页显示正确

**Step 3:** Commit

```bash
git add . && git commit -m "feat: homepage with three entry cards"
```

---

## Phase 3: 改造家居页（搜索 + 分页）

### Task 3.1: 改造家居列表页框架

**Files:**
- Create: `src/pages/products/index.astro`
- Create: `src/pages/products/[category].astro`（可选：按分类路由）
- Create: `src/components/ProductCard.astro`

**Step 1:** 创建 ProductCard

```astro
---
interface Props {
  name: string;
  venue: string;
  audience: string;
  material: string;
  image: string;
}
const { name, venue, audience, material, image } = Astro.props;
---
<article class="bg-white rounded-lg border border-slate-200 overflow-hidden">
  <img src={image} alt={name} class="w-full h-40 object-cover bg-slate-100" />
  <div class="p-4">
    <h3 class="font-semibold text-slate-800">{name}</h3>
    <p class="mt-1 text-sm text-slate-600"><span class="text-slate-500">适用场所：</span>{venue}</p>
    <p class="mt-1 text-sm text-slate-600"><span class="text-slate-500">适用人群：</span>{audience}</p>
    <p class="mt-2 text-sm text-slate-600 line-clamp-2">{material}</p>
  </div>
</article>
```

**Step 2:** 创建 `/products` 页面，加载 products.json，渲染 ProductCard 网格

**Step 3:** 验证

**Step 4:** Commit

```bash
git add . && git commit -m "feat: products list page with ProductCard"
```

---

### Task 3.2: 添加搜索框（客户端筛选）

**Files:**
- Modify: `src/pages/products/index.astro`
- Create: `src/components/ProductSearch.astro`（客户端 island）

**Step 1:** 使用 Astro 的 `client:load` 创建搜索组件，按 name/venue/audience 过滤产品列表

```astro
---
// ProductSearch.astro - 使用 client:load 以启用交互
---
<div class="mb-6">
  <input type="search" id="product-search" placeholder="搜索产品名称、适用场所、适用人群..."
    class="w-full px-4 py-2 border border-slate-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500" />
</div>
<div id="product-list"></div>
<div id="pagination"></div>
<script>
  // 从 data 属性或全局变量读取 products，实现筛选和分页
</script>
```

**Step 2:** 将 products 通过 `define:vars` 或 script 注入，实现即时过滤

**Step 3:** 验证搜索、清空后列表恢复

**Step 4:** Commit

```bash
git add . && git commit -m "feat: product search filter"
```

---

### Task 3.3: 添加分页

**Files:**
- Modify: `src/pages/products/index.astro` 或 ProductSearch 组件

**Step 1:** 在搜索组件内实现分页逻辑（每页 12 条），渲染上一页/下一页或页码

**Step 2:** 确保搜索与分页联动（筛选后再分页）

**Step 3:** 验证

**Step 4:** Commit

```bash
git add . && git commit -m "feat: products pagination"
```

---

## Phase 4: 政策页

### Task 4.1: 政策列表页

**Files:**
- Create: `src/pages/policies/index.astro`

**Step 1:** 加载 policies.json，渲染为链接列表（标题 + 下载/新标签打开 PDF）

```astro
---
import BaseLayout from '../../layouts/BaseLayout.astro';
import policies from '../../data/policies.json';
---
<BaseLayout title="家居改造政策信息 - 无碍e家">
  <h1 class="text-2xl font-bold text-slate-800 mb-6">家居改造政策信息</h1>
  <ul class="space-y-3">
    {policies.map(p => (
      <li>
        <a href={p.file} target="_blank" rel="noopener" class="block p-4 bg-white rounded-lg border border-slate-200 hover:border-blue-300 hover:shadow transition">
          {p.title}
        </a>
      </li>
    ))}
  </ul>
</BaseLayout>
```

**Step 2:** 验证点击可打开 PDF

**Step 3:** Commit

```bash
git add . && git commit -m "feat: policies page"
```

---

## Phase 5: 经验分享页（书籍 + 分页）

### Task 5.1: 书籍列表与分页

**Files:**
- Create: `src/pages/books/index.astro`
- Create: `src/components/BookCard.astro`

**Step 1:** BookCard 展示封面、标题、简介、作者、适用人群

**Step 2:** 书籍页加载 books.json，实现客户端分页（每页 12 条）

**Step 3:** 验证

**Step 4:** Commit

```bash
git add . && git commit -m "feat: books page with pagination"
```

---

## Phase 6: 搜索增强（可选 Pagefind）

### Task 6.1: 集成 Pagefind

**Files:**
- Modify: `package.json`
- Modify: `astro.config.mjs`

**Step 1:** 安装 `@pagefind/cli`，在 build 后运行 pagefind

```json
// package.json scripts
"build": "astro build && pagefind --site dist"
```

**Step 2:** 在 products 页引入 Pagefind UI（或保留原有客户端筛选，二选一）

**说明：** 若产品量不大（<100），客户端筛选已够用，可跳过 Pagefind。

---

## Phase 7: 部署

### Task 7.1: 配置 Vercel 部署

**Files:**
- Create: `vercel.json` 或使用默认
- Modify: `package.json`（确保 build 正确）

**Step 1:** 安装 Vercel adapter

```bash
npx astro add vercel
```

**Step 2:** 本地构建验证

```bash
npm run build
npm run preview
```

Expected: 构建成功，预览正常

**Step 3:** 推送至 Git 后连接 Vercel 项目

**Step 4:** Commit

```bash
git add . && git commit -m "chore: add Vercel deployment config"
```

---

## 验收清单

- [ ] 首页三个入口可跳转
- [ ] 改造家居：产品卡片、搜索、分页
- [ ] 政策：PDF 链接可打开
- [ ] 经验分享：书籍卡片、分页
- [ ] 蓝白简约风格一致
- [ ] 移动端基本适配
- [ ] `npm run build` 成功

---

## 执行选项

**1. 本会话逐个执行** — 我按任务顺序逐步实现，每步完成后你复核

**2. 新会话批量执行** — 在新会话中打开 `executing-plans` skill，按检查点批量执行

你希望用哪种方式？
